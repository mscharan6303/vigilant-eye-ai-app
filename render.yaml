services:
  - type: web
    name: vigilant-eye-backend
    env: node
    buildCommand: npm install
    startCommand: node backend/server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
